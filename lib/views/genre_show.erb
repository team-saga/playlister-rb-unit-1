<!DOCTYPE html>
<html>
<head>
  <title><%= @genre.name %></title>
</head>
<body>
<p><a href="../index.html" title=""><< All Songs</a></p>
<h1><%= @genre.name %> - <%= @genre.songs.count %> Song(s), <%= @genre.artists.count %> Artist(s)</h1>
<ol>
	<% @genre.songs.each do |song| %>
   <li><a href="../artists/<%= song.artist.url%>.html" title=""><%= song.artist.name %></a> - <a href="../songs/<%= song.url%>.html" title=""><%= song.name %></a></li>
  <% end %>
</ol>
</body>
</html>